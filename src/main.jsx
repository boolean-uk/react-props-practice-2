import React from 'react'
import ReactDOM from 'react-dom/client'
import ChatManager from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ChatManager />
  </React.StrictMode>,
)
